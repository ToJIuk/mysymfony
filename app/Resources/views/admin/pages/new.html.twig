{% extends 'base.html.twig' %}
{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-xs-12">
                <h1>New Pages</h1>
                {{ form_start(pagesForm) }}
                {{ form_widget(pagesForm) }}
                <button type="submit" class="btn btn-primary">Save</button>
                {{ form_end(pagesForm) }}
            </div>
        </div>
    </div>
{% endblock %}